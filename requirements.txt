fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
# pydantic bumped 2.7.1 -> 2.13.4 to satisfy langchain-core (>=2.7.4). Still
# compatible with fastapi 0.111 (pydantic<3) and pydantic-settings.
pydantic[email]==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.0.1
python-multipart==0.0.9
pytest==8.2.0
httpx==0.27.0
joblib==1.4.2
# ─── Trained classifiers (Models 1–3) ──────────────────────────────────────
# Pin the sklearn/xgboost runtime so it no longer floats (it previously resolved
# transitively to whatever was latest, risking silent prediction drift on the
# pickled pipelines). NOTE: the committed .pkl artifacts are internally
# inconsistent — safety_classifier was saved under sklearn 1.9.0, topic under
# 1.7.0 (verified from the pickle bytes). 1.9.0 loads all three with verified-
# identical predictions (topic emits a benign InconsistentVersionWarning). The
# proper long-term fix is to re-export all models from srh-ml-model under one
# pinned environment; until then this pin is the stable, prediction-verified set.
scikit-learn==1.9.0
xgboost==3.2.0

# ─── RAG / LLM layer ───────────────────────────────────────────────────────
# Versions below are the resolved set installed into the venv (Part 6.2).
langchain==1.3.11
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
# Embeddings (local) — paraphrase-multilingual-MiniLM-L12-v2
sentence-transformers==5.5.1
huggingface-hub==1.19.0
# Vector stores: Pinecone (cloud, modern SDK) + Chroma (local dev/CI)
pinecone==9.1.0
chromadb==1.5.9
# Knowledge base ingestion
pypdf==6.14.2
tqdm==4.68.2
# Language detection proxy (until the language classifier is swapped in)
langdetect==1.0.9
# LLM benchmarking (Part 5)
rouge-score==0.1.2
